{
    "private": true,
    "type": "module",
    "scripts": {
        "start:dev": "php artisan serve",
        "refresh": "php artisan migrate:refresh --seed",
        "seed": "php artisan db:seed",
        "migration": "php artisan migrate",
        "migration:reset": "php artisan migrate:reset",
        "test": "php artisan test --coverage --coverage-html=coverage",
        "dev": "vite",
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.6.1",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0"
    }
}
